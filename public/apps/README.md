# Apps Folder Convention

Each embedded app should follow this structure:

- `public/apps/<app-name>/index.html`
- `public/apps/<app-name>/assets/*` (if the app has built assets)

Current apps:

- `public/apps/photographer/index.html`
- `public/apps/story-builder/index.html`
